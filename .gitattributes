# Normalise to LF in the repository. Without this, files authored on Windows
# reach Ubuntu CI runners with CRLF, which breaks shell scripts in workflows.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.joblib binary
